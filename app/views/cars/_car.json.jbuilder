json.extract! car, :id, :patent, :brand, :model_car, :string, :seats, :transmision, :fuel, :is_available, :is_enable, :color, :car_number, :year_model, :integer, :created_at, :updated_at
json.url car_url(car, format: :json)
