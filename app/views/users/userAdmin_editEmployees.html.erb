<nav class="navbar navbar-light is-warning">
  <a class="navbar-brand" href="">
    <%= image_submit_tag('ACA.png')%>
    <div class= "text-right">
         <%= link_to "Cerrar sesión", destroy_user_session_path, 'data-turbo.method': :delete, class:"button is-warning is-outlined is-light"%>
    </div>
  </a>
</nav>
<section class="hero is-warning is-fullheight"> 
<div class="hero-body text-center ">
        <div class="container ">
        <h2 class = "text-center">Editar Supervisor</h2><br>
            <div class="columns is-centered">
                <div class="column is-5-tablet is-4-desktop is-4-widescreen box">
                   

                    <%= form_for @supervisor do |s| %>
                              <% flash.each do |type, msg| %>
                                     <%= msg %>
                              <% end %>
                        <div class="field has-text-weight-bold has-text-centered">
                            <%= s.label :name,"Nombre" %><br>
                            <%= s.text_field :name %>
                        </div>

                        <div class="field has-text-weight-bold has-text-centered">
                            <%= s.label :last_name,"Apellido" %><br>
                            <%= s.text_field :last_name %>
                        </div>

                        <div class="field has-text-weight-bold has-text-centered">
                          <%= s.label :dni_nro,"DNI" %><br>
                          <%= s.text_field :dni_nro %>
                        </div>

                        <div class="field has-text-weight-bold has-text-centered actions">
                            <%= s.submit "Confirmar",class: "button is-warning is-outlined is-light",data: { turbo_confirm:'Estas seguro de que desea guardar cambios?' } %>
                        </div>

                    <% end %><br>
                    <div class="text-center">
                      <%= link_to "Volver", user_admin_lista_path,class: "button is-warning is-outlined is-light" %> 
                    </div>
                </div>
            </div>
        </div>
    </div>
</section> 

